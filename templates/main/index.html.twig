{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/Users/whoami/Documents/GitHub/TrabzonDMT-site/src/Controller/MainController.php'|file_link(0) }}">src/Controller/MainController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/Users/whoami/Documents/GitHub/TrabzonDMT-site/templates/main/index.html.twig'|file_link(0) }}">templates/main/index.html.twig</a></code></li>
    </ul>
    {% if date %}
        <h2>Day: {{ date.day }}, Month: {{ date.month }}</h2>
        {% for event in date.events %}
            <h3>{{ event.title }}</h3>
            <p><b>Summary: </b> {{ event.summary }}</p>
            <p><b>Cotent:</b> {{ event.content }}</p>
        {% else %}
            <p>No events found</p>
        {% endfor %}
    {% endif %}
</div>
{% endblock %}
